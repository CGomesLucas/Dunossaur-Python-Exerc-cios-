letras = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J']

# não imprime a, e, i

for letra in letras:
    if letra != 'A' and letra != 'E' and letra != 'I' and letra != 'O' and letra != 'U':
        print(letra)
